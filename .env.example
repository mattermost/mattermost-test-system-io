# ==============================================================================
# Test System IO Server - Environment Configuration
# ==============================================================================
#
# DEVELOPMENT: Only RUST_ENV is required. All other values use sensible defaults.
# PRODUCTION:  RUST_ENV, TSIO_DATABASE_URL, and TSIO_API_KEY are required.
#              Server will NOT start if production values match development defaults.
#
# ==============================================================================

# Environment (REQUIRED)
# Options: development, production (or dev, prod)
RUST_ENV=development

# ==============================================================================
# PRODUCTION CONFIGURATION (uncomment and set these for production)
# ==============================================================================

# Database (REQUIRED in production)
# PostgreSQL connection string:
# TSIO_DATABASE_URL=postgres://user:password@localhost:5432/tsio

# API Key (REQUIRED in production - must be at least 32 characters)
# TSIO_API_KEY=your-secure-api-key-at-least-32-characters-long

# ==============================================================================
# OPTIONAL CONFIGURATION (defaults shown)
# ==============================================================================

# Server host and port
# TSIO_SERVER_HOST=127.0.0.1
# TSIO_SERVER_PORT=8080

# Allowed CORS origins in production (comma-separated, REQUIRED for cross-origin access)
# In development, http://localhost:3000 is always allowed automatically.
# In production, set this to your frontend URL(s):
# TSIO_SERVER_ALLOWED_ORIGINS=https://tsio.example.com

# Static assets directory (for serving frontend in production)
# TSIO_STATIC_DIR=/app/static

# Maximum upload size in bytes (default: 50MB)
# TSIO_MAX_UPLOAD_SIZE=52428800

# Maximum concurrent uploads (default: 10)
# Bounds memory usage: max_concurrent_uploads × max_upload_size
# TSIO_MAX_CONCURRENT_UPLOADS=10

# Artifact retention in hours (default: 1 hour dev, 168 hours/7 days prod)
# TSIO_ARTIFACT_RETENTION_HOURS=168

# Logging level (standard RUST_LOG env var)
# RUST_LOG=info,actix_web=info,tsio=debug
