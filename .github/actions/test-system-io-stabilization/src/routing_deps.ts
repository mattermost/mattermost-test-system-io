/** Vendored routing decision (W11) — same self-containment rule as the ban
 * checker: the action directory downloads without the sibling action. */
export { routeVerdict, resolveOwner } from "./vendor/routing.ts";
